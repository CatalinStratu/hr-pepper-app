<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PepperHRInterview"
        android:usesCleartextTraffic="true"
        tools:targetApi="31"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory">

        <!-- Main Activity - Interview Start Screen -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Robot focus handling -->
            <meta-data
                android:name="com.aldebaran.qisdk.action.ROBOT_FOCUS"
                android:value="true" />
        </activity>

        <!-- Interview Session Activity -->
        <activity
            android:name=".activities.InterviewActivity"
            android:screenOrientation="landscape">
            <meta-data
                android:name="com.aldebaran.qisdk.action.ROBOT_FOCUS"
                android:value="true" />
        </activity>

        <!-- Interview Complete Activity -->
        <activity
            android:name=".activities.CompleteActivity"
            android:screenOrientation="landscape">
            <meta-data
                android:name="com.aldebaran.qisdk.action.ROBOT_FOCUS"
                android:value="true" />
        </activity>

    </application>

</manifest>
